
name of type string ==> varchar in oracle
                        char[] in mysql
                         text in postgres

Using Postgres Sql:-
inserting values into table:  
  insert into student values (1,'Vipul',80);

fetching date from table:-
  select sname from student where sid=1;
  select * from student; // to fetch all the data


postgres jdbc driver
https://jdbc.postgresql.org/
https://mvnrepository.com/artifact/org.postgresql/postgresql

rs.next()--> it will shift by 1 pointer and it will check whether the next row is there or not

  
